import { Button, ContainedBadge } from '@components/Common';
import styled from 'styled-components';

export const CategoryBadge = styled(ContainedBadge)`
  margin: 1.6rem 0 2.4rem 0;
`;

export const MatchingButton = styled(Button)`
  position: fixed;
  bottom: 2.4rem;
  margin: 0 1.6rem;
`;
